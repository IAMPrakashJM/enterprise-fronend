# Illustrated PDF verification — 8 September 2026

The generated guide has 38 pages, 19 chapter bookmarks and 19 actual browser
screenshots. All numbered figure captions appear in order and every original PNG
is included in screenshots/. Contents links resolve to all 19 chapter destinations.
Text extraction succeeds without replacement characters; all text and images stay
within printable page bounds. Unchanged input generates identical PDF bytes.

Screenshots were captured from the desktop browser shell at 1440×1000 using a
new isolated API store with fictional seeded/test data. The full capture completed
without browser runtime errors and demonstrated views, panels, CSV validation/
confirmation/results, approval stages, role-specific inbox, history and requester
notifications. Navigation images were recaptured with tighter dialog/login framing.
No production data was changed and no production credentials were captured.

All pages were rendered and inspected in contact sheets; key screen pages were
also inspected at full size. Figures have explanatory captions and are kept with
their images. Original PNGs remain available for zooming separately from the PDF.

The source distinguishes committed Monday history from later deployed working-tree
development. The screenshot pass also documented the older form-menu demo actions
as pending and directs users to the working Record approval section. No new full
application test run was claimed for this documentation update.

Generated with ReportLab 5.0.1, DejaVu Sans and Python 3.12; verified/rendered with
PyMuPDF 1.28.2. Rebuild and capture instructions are in README.md.
